# Runtime dependencies for the API that backend.py / gunicorn serves.
# The served model is a scikit-learn soft-voting ensemble, so nothing here
# needs xgboost, lightgbm, catboost or optuna. Training extras live in
# requirements-ml.txt and are deliberately NOT installed on the server:
# a broken training dependency must never be able to block a deploy.

pandas==2.1.3
numpy==1.26.2
scikit-learn==1.5.2
scipy==1.11.4
joblib==1.3.2

Flask==3.1.3
Flask-CORS==4.0.0
gunicorn==21.2.0
